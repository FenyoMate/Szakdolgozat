export class tunedModel{
   id: number;
   model_name: string;
   model_id: string;

   constructor(id: number, model_name: string, model_id: string) {
       this.id = id;
       this.model_name = model_name;
       this.model_id = model_id;
   }
}
